# Application Name
spring.application.name=leave-management-system

# Server Port
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/leave_management_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Shreyash14@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
jwt.expiration=86400000

# Email Configuration (SMTP - Gmail Example)
# Email Configuration (SMTP - Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shreyash14012005@gmail.com
spring.mail.password=hbbmvvtcnxgcjvxs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Email From Address - MUST match Gmail account
app.email.from=shreyash14012005@gmail.com

# Email Mode (false = send real emails, true = mock/log only)
app.email.mock=false


# Leave Policy Configuration
app.leave.annual-entitlement=24
app.leave.max-auto-approvals-per-month=2
app.leave.auto-approval-threshold=2
app.leave.carry-forward-max=12
app.leave.encashment-max=10

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# Logging
logging.level.org.springframework.web=INFO
logging.level.org.springframework.security=DEBUG
logging.level.com.company.leave=DEBUG

# Date Format
spring.jackson.date-format=yyyy-MM-dd
spring.jackson.time-zone=UTC

# Enable response compression
server.compression.enabled=true